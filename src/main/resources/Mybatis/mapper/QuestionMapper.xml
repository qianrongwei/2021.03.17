<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qrw.mapper.QuestionMapper">

    <resultMap id="questionMap" type="question">
        <id column="id" property="id"></id>
        <result column="title" property="title"></result>
        <result column="description" property="description"></result>
        <result column="tag" property="tag"></result>
        <result column="gmt_create" property="gmtCreate"></result>
        <result column="gmt_modified" property="gmtModified"></result>
        <result column="comment_count" property="commentCount"></result>
        <result column="view_count" property="viewCount"></result>
        <result column="like_count" property="likeCount"></result>
    </resultMap>


    <insert id="insertQuestion" parameterType="question">
        insert into question(title,description,tag,creator,gmt_create,gmt_modified)
        values(#{title},#{description},#{tag},#{creator},#{gmtCreate},#{gmtModified});
    </insert>

    <select id="list" resultMap="questionMap" parameterType="Integer" >
        select * from question limit #{offset},2;
    </select>

    <select id="getCount" resultType="Integer">
        select count(1) from question;
    </select>

</mapper>